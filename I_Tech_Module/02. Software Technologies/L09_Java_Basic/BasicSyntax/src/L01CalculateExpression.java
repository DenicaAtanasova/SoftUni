public class L01CalculateExpression {
    public static void main(String[] args) {
        double val = ((30 + 21) * 1 / 2 * (35 - 12 - 1 / 2));
        double sqrVal = Math.pow(val, 2);
        System.out.println(sqrVal);
    }
}
